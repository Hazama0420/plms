# PHASE 6 — UI/UX V2 MIGRATION MATRIX

> **Document Type:** Production Implementation & Component Migration Matrix  
> **Target Release:** Inland Property Design System V2 (Phase 6)  
> **Source Documents:** `INLAND_PROJECT_CONTEXT.md`, `INLAND_DESIGN_SYSTEM.md`, `INLAND_DESIGN_SYSTEM_V2.md`, `INLAND_V2_PROTOTYPE_VALIDATION.md`  
> **Strict Rule:** Frozen security, authentication, RLS, server actions, and business logic MUST remain untouched.

---

## 1. Comprehensive Component & Surface Migration Matrix

| Area | Current Production Reality | V2 Target Contract | Implementation Gap | Risk Level | Target Production Files | Migration Strategy | Status |
|---|---|---|---|:---:|---|---|:---:|
| **Color Architecture** | Default Emerald with dormant `data-accent="blue"` and `data-accent="purple"` tokens; literal `#0E2C24` and `#E2B23B` hardcoded outside CSS variables. | Single action Emerald (`#059669`/`#10b981`); official tokens for Forest Green (`#0E2C24`) and Warm Gold (`#E2B23B`); jewel-box dark mode. | Retired blue/purple accent CSS; expose `--color-inland-forest` & `--color-inland-gold` in `@theme inline`. | **Low** | `app/globals.css` | Deprecate blue/purple; add corporate CSS variables to `:root` & `.dark`. | **PHASE 6.1** |
| **Typography & Numbers** | Inter sans-serif; manual formatting; inconsistent `tabular-nums` on table numbers and prices. | Universal `tabular-nums` on all IDR currency, metrics, listing codes, and table figures; 3-tier root font scale. | Ensure `tabular-nums` is baked into price renderers and KPI cards. | **Low** | `app/globals.css`, `lib/kpr.ts`, domain components | Keep Inter; codify tabular figures utility and type scale tokens. | **PHASE 6.1** |
| **Radius Scale** | Root declared `--radius: 0.5rem` (8px); ad-hoc mix of `rounded-md`, `rounded-xl`, `rounded-2xl`, `rounded-3xl` in components. | Strict 5-tier radius curve: 6px (Chips), 8px (Dense), 12px (Form/Modal), 16px (Card), 24px (Hero Shell), Full (Pills). | Document and align component classes to the 5-tier scale. | **Low** | `app/globals.css`, `components/ui/*` | Standardize utility class mappings without breaking existing layout borders. | **PHASE 6.1** |
| **Density & Sizing Tokens** | Primitives default to `h-8` (32px); page forms constantly override with ad-hoc `h-10 px-3 rounded-xl`. | Strict 2-tier scale: Tier 1 (`h-10` / 40px) for storefront/forms; Tier 2 (`h-8` / 32px) for tables/Kanban; Tier 3 (`h-6` / 24px) for micro chips. | Base `button.tsx` & `input.tsx` default to `h-10`, with `size="sm"` (`h-8`) for dense contexts. | **Medium** | `components/ui/button.tsx`, `components/ui/input.tsx` | Ensure default size is 40px while auditing all dense consumers to pass `size="sm"`. | **PHASE 6.2** |
| **Button Primitive** | Base UI button defaults to `h-8 rounded-lg`; active `translate-y-1px`. | `default` is `h-10 rounded-xl` (Tier 1); `sm` is `h-8 rounded-lg` (Tier 2); accessible labels on `size="icon"`. | Audit all button consumers before changing default height; provide backward-compatible sizing props. | **Medium** | `components/ui/button.tsx` | Add explicit `default` size as `h-10`, verify table/toolbar buttons use `size="sm"`. | **PHASE 6.2** |
| **Input / Textarea** | `input.tsx` defaults to `h-8 rounded-lg`; `textarea.tsx` is `min-h-16 rounded-lg`. | `input.tsx` defaults to `h-10 rounded-xl`; `textarea.tsx` defaults to `min-h-20 rounded-xl`. | Forms currently using ad-hoc classes will align with default primitive. | **Medium** | `components/ui/input.tsx`, `components/ui/textarea.tsx` | Standardize default input styling to 40px; support `size="sm"` (32px) for table filters. | **PHASE 6.2** |
| **Badge Primitive** | Single `h-5 rounded-4xl` pill primitive; feature pages mix manual classes (`rounded-md`, `rounded-lg`). | Strict 2-tier badge grammar: Lifecycle status = `rounded-full` with translucent tint; Technical metadata = `rounded-md font-mono`. | Add `variant="status"` (pill) and `variant="metadata"` (chip) or standard classes. | **Low** | `components/ui/badge.tsx` | Add variant prop options while preserving existing badge behavior. | **PHASE 6.2** |
| **PropertyCard** | Two separate components: `PropertyCard.tsx` (16:9 catalog) vs `DashboardPropertyCard.tsx` (16:10 dashboard). | Single polymorphic `PropertyCard` with 4 variants: `catalog`, `dashboard`, `manage`, `compact`. Standard 16:9 media. | Merge visual features of `DashboardPropertyCard` into `PropertyCard` as `variant="dashboard"`. | **Medium** | `components/properties/PropertyCard.tsx`, `components/dashboard/DashboardPropertyCard.tsx` | Upgrade `PropertyCard.tsx` with variant prop; migrate consumers; deprecate old card. | **PHASE 6.2 & 6.3** |
| **Pagination** | `ui/pagination.tsx` (raw primitive) vs `NumberedPagination.tsx` (URL-query synchronized). | Canonicalize `NumberedPagination.tsx` for all query-driven listing pages. | `ui/pagination.tsx` should only serve as the internal primitive of `NumberedPagination`. | **Low** | `components/properties/NumberedPagination.tsx`, `components/ui/pagination.tsx` | Keep `NumberedPagination` as public API; ensure accessible Indonesian labels. | **PHASE 6.2** |
| **LeadCaptureModal** | Uses native `<input>` and `<textarea>` with ad-hoc classes; missing label `htmlFor`. | Fully refactored to use `@/components/ui/input`, `@/components/ui/textarea`, and proper accessible labels. | Replace raw HTML tags with UI primitives; connect label `htmlFor` with unique IDs. | **Low** | `components/inquiry/LeadCaptureModal.tsx` | Refactor internals without modifying server lead generation API. | **PHASE 6.2** |
| **Public Top Navigation** | Header uses slide-out Sheet drawer for everyone; desktop center has quick pills, but no full navbar. | Clean consumer top navbar for guests/viewers (`Beranda`, `Properti`, `KPR`, `Survei`, `Kontak`, `Masuk`). | Split navigation layout: Storefront header for public visitors vs ERP sidebar for staff. | **Medium** | `app/(dashboard)/layout.tsx`, `components/layout/StorefrontNavbar.tsx` | Implement role/session-aware layout: public gets clean navbar, staff gets ERP sidebar. | **PHASE 6.3 & 6.4** |
| **Public Discovery Hero** | `PageHeader.tsx` uses `/bg-header.webp` with `rounded-[1.5rem]`; search bar stacks 3 inputs on mobile. | `PageHeader.tsx` preserved with gold hairline; mobile search collapses to 1 input + filter drawer. | Implement mobile filter drawer trigger on small viewports (< 640px). | **Low** | `components/dashboard/PageHeader.tsx`, `app/(dashboard)/dashboard/page.tsx` | Add responsive mobile search trigger while preserving desktop multi-field toolbar. | **PHASE 6.3** |
| **Property Catalog** | Catalog grid has filter toolbar and `NumberedPagination`; card hover is basic. | Responsive 1 -> 2 -> 3 -> 4 grid with canonical `PropertyCard variant="catalog"`; smooth hover lift. | Ensure card grid spacing is `gap-4 sm:gap-6`; entire card is clean click target. | **Low** | `app/(dashboard)/properties/page.tsx`, `components/properties/PropertyCard.tsx` | Connect polymorphic card variant="catalog". | **PHASE 6.3** |
| **Property Detail View** | Modular detail components in `components/property-detail/` (Gallery, Specs, Header, Agent Card, KPR). | Preserve confirmed layout (PASSED prototype); ensure 16:9 watermarked gallery & canonical specs icons. | Already ratified as PASS in prototype validation. Minimal changes required. | **Low** | `components/property-detail/*`, `app/(dashboard)/properties/[id]/page.tsx` | Standardize specs icons to canonical Lucide set (`Bed`, `Bath`, `Building2`, `Maximize2`). | **PHASE 6.3** |
| **Corporate Footer** | `SiteFooter.tsx` uses hardcoded `#0E2C24` and `#E2B23B`; negative margins inside `<main>`. | Use CSS variables `--color-inland-forest` & `--color-inland-gold`; clean full-width placement. | Replace raw hex with CSS variable tokens; ensure jewel-box dark mode contrast. | **Low** | `components/layout/SiteFooter.tsx` | Update classes to `bg-inland-forest text-white border-t border-inland-gold/30`. | **PHASE 6.3** |
| **ERP Persistent Sidebar** | Slide-out Sheet drawer triggered via hamburger button on all desktop views. | Persistent collapsible sidebar (`w-64` expanded / `w-16` icon-collapsed) for authenticated staff. | Build persistent desktop sidebar component; auto-collapse to `w-16` on viewports < 1024px. | **High** | `app/(dashboard)/layout.tsx`, `components/dashboard/app-sidebar.tsx` | Create `PersistentAppSidebar.tsx`; wrap dashboard layout with responsive split-pane shell. | **PHASE 6.4** |
| **OperationalHeader** | Missing component; ERP pages use ad-hoc `<h1>` headers or plain text breadcrumbs. | New `OperationalHeader.tsx` with breadcrumbs, live title, context KPI summary, and action toolbar. | Build reusable `OperationalHeader.tsx` and integrate across `/crm`, `/invoices`, `/projects`, `/admin`. | **Medium** | `components/layout/OperationalHeader.tsx` (New) | Build new canonical ERP header component; adopt across all operational routes. | **PHASE 6.4** |
| **Staff Dashboard** | Currently combines hero search with admin stats in `DashboardViewSelector.tsx`. | Clean separation: `ViewerDashboardView` (Storefront hero) vs `OperationalDashboardView` (Command Desk). | Structure dashboard content cleanly based on user role (Staff vs Guest). | **Medium** | `components/dashboard/DashboardViewSelector.tsx`, `components/dashboard/AdminStatsGrid.tsx` | Adopt `OperationalHeader` + 4-column KPI stats grid for staff view. | **PHASE 6.5** |
| **CRM Lead Pipeline** | 7-column Kanban with HTML5 drag-and-drop; mobile renders single stage tab switcher. | Desktop Kanban enforces `w-[260px]` columns with custom horizontal scroll; mobile adds swipe gestures. | Add custom horizontal scrollbar utility; verify role-aware phone masking remains strict. | **Medium** | `components/crm/CrmKanbanBoard.tsx`, `app/(dashboard)/crm/page.tsx` | Refactor visual styles of column containers and cards without touching drag-and-drop actions. | **PHASE 6.5** |
| **Invoices Table** | Table with status pills, search bar, and action dropdown. | High-density Tier 2 (`h-8`) table; compact status badges; clean CSV export & print toolbar. | Standardize table row padding and status badge classes. | **Low** | `app/(dashboard)/invoices/page.tsx` | Refactor toolbar and table rows to Tier 2 density. | **PHASE 6.5** |
| **Remaining ERP Surfaces** | Surveys, Projects, Reports, Settings currently have varying card and header styles. | Unified under `OperationalHeader` + 5-tier radius + 2-tier density. | Standardize headers, filters, and KPI cards across each module. | **Low** | `app/(dashboard)/surveys/*`, `projects/*`, `reports/*`, `settings/*` | Step-by-step migration per module. | **PHASE 6.5** |
